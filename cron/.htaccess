# Protect the cron folder from web access (config keys, logs, queue).
# Server-side includes from /admin still work — this only blocks browser access.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
